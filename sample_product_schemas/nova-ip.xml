<productSchema xmlns="http://docs.rackspace.com/core/usage/schema"
               namespace="http://docs.rackspace.com/usage/nova/ip"
               serviceCode="CloudServersOpenStack"
               version="1" resourceTypes="INSTANCE"
               type="USAGE">
    <description>
        Cloud Server OpenStack Additional IP Usage Event.
    </description>

    <attribute name="ip" type="string" use="required">
        IP address of the Cloud Server.
    </attribute>

    <attribute name="serverId" type="int UUID">
        The serverId that this IP is associated to.
    </attribute>
    
    <attribute name="serverName" type="string">
        The name of the server that this IP is associated to.
    </attribute>
    
    <attribute name="reserved" type="boolean" default="false">
        A flag to indicate whether the IP is reserved or not. 
    </attribute>
         
</productSchema>
