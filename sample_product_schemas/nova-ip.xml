<productSchema xmlns="http://docs.rackspace.com/core/usage/schema"
               namespace="http://docs.rackspace.com/usage/nova/ip"
               serviceCode="CloudServersOpenStack"
               version="1" resourceTypes="INSTANCE"
               type="USAGE">
    <description>
        Specifies a usage event associating an additional IP with a Cloud Server in OpenStack.
    </description>

    <attribute name="ip" type="string" use="required">
        Specifies the IP address of the Cloud Server.
    </attribute>

    <attribute name="serverId" type="int UUID" use="optional">
        Specifies the serverId that this IP is associated to.
    </attribute>
    
    <attribute name="serverName" type="string" use="optional">
        Specifies the name of the server that this IP is associated to.
    </attribute>
    
    <attribute name="reserved" type="boolean" use="optional" default="false">
        Specifies a flag that indicates whether the IP is reserved or not. 
    </attribute>
         
</productSchema>
