<productSchema xmlns="http://docs.rackspace.com/core/usage/schema"
               namespace="http://docs.rackspace.com/usage/cbs"
               serviceCode="CloudBlockStorage"
               version="1"
               resourceTypes="VOLUME">
    <description>
        CBS specific usage fields related to the volume.
    </description>
    <attribute name="type" type="string" use="required"
               allowedValues="SATA SSD">
        Describes the type of volume used.  SSD is high performance
        based.  SATA is standard.
    </attribute>
    <attribute name="provisioned" type="int" use="required"
               unitOfMeasure="GB" min="0" max="1024">
        The amount of storage provisioned for the volume.
    </attribute>
</productSchema>
