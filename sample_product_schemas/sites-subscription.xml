<productSchema xmlns="http://docs.rackspace.com/core/usage/schema"
        namespace="http://docs.rackspace.com/usage/sites/subscription"
        version="1"
        serviceCode="CloudSites" type="USAGE_SNAPSHOT"
        resourceTypes="SITES_SUBSCRIPTION">
    <description>
        Subscription to Cloud Sites.
    </description>
    <attribute name="isNewAccount" type="boolean" use="optional" default="false"
               displayName="Is new account">
        A boolean. Should be true if the date the plan is added is the same UTC date the account was created. 
        For events originating from Sign Up Facade, this is always true.
        For events originating from Cloud Control, this may be true or false.
    </attribute>
</productSchema>
