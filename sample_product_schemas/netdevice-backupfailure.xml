<productSchema xmlns="http://docs.rackspace.com/core/usage/schema"
        namespace="http://docs.rackspace.com/event/netdevice/backupfailure"
        serviceCode="NetDevice"
        version="1"
        resourceTypes="LOADBALANCER FIREWALL"
        type="INFO">

    <description>
        INFO messages on loadbalancer and firewall backup failures for FDaaT.
    </description>

    <attribute name="deviceHostname" type="string"
               use="required">
        The hostname of the device.
    </attribute>

    <attribute name="url" type="anyURI"
               use="required">
        The link to view the event info in Torq.
    </attribute>

    <attribute name="deviceNumber" type="string"
               maxLength="64"
               use="required">
        The device number.
    </attribute>

    <attribute name="user" type="string"
               maxLength="32"
               use="required">
        The User that created the event.
    </attribute>

    <attribute name="failureType" type="Name"
               use="required"
               allowedValues="CONNECTION_TIMEOUT CONNECTION_REFUSED DEVICE_TIMEOUT DEVICE_LOGIN_FAILED DEVICE_BAD_CONFIG DEVICE_BAD_STATE TFTP_CONNECTION_ERROR SSH_CONNECTION_ERROR SSL_CERTIFICATE_ERROR INTERNAL_ERROR OTHER_ERROR UNKNOWN_ERROR">
        The type of failure.
    </attribute>

</productSchema>
